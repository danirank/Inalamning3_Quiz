<UserControl x:Class="QuizInlamning3.View.MenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuizInlamning3.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="#1E3A5F">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="40"/>
        </Grid.ColumnDefinitions>

        <!-- Titel -->
        <Border Grid.Row="1" Grid.Column="1"
                Background="#2E4A73"
                CornerRadius="8"
                Padding="16"
                Margin="0,0,0,20"
                HorizontalAlignment="Center">
            <TextBlock Text="QuizGame"
                       Foreground="WhiteSmoke"
                       FontSize="30"
                       FontWeight="SemiBold"
                       TextAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>

        <!-- Knappar -->
        <StackPanel Grid.Row="2" Grid.Column="1"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,40">

            <Button x:Name="playBtn"
                    Click="playBtn_Click"
                    Content="Spela Quiz"
                    Background="#4C74B9"
                    Foreground="White"
                    Padding="14,8"
                    Margin="0,6"
                    Width="200"
                    FontSize="16"
                    BorderBrush="#395A93"
                    BorderThickness="1"
                    Cursor="Hand"/>

            <Button x:Name="editBtn"
                    Click="editBtn_Click"
                    Content="Redigera/Skapa  Quiz"
                    Background="#4C74B9"
                    Foreground="White"
                    Padding="14,8"
                    Margin="0,6"
                    Width="200"
                    FontSize="16"
                    BorderBrush="#395A93"
                    BorderThickness="1"
                    Cursor="Hand"/>

            <Button x:Name="leaderboardBtn"
                    Click="leaderboardBtn_Click"
                    Content="Visa Resultatlista"
                    Background="#4C74B9"
                    Foreground="White"
                    Padding="14,8"
                    Margin="0,6"
                    Width="200"
                    FontSize="16"
                    BorderBrush="#395A93"
                    BorderThickness="1"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</UserControl>
