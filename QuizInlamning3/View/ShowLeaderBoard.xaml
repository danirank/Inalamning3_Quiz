<UserControl x:Class="QuizInlamning3.View.ShowLeaderBoard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuizInlamning3.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>
        
        <!--Rubrik-->
        <Border Grid.Row ="0">
            <TextBlock FontSize="40" VerticalAlignment="Center" HorizontalAlignment="Center">Leaderboard</TextBlock>
        </Border>
        
        <!--Rubrikrad-->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" >
                <TextBlock FontSize="16" FontWeight="Bold">Rank</TextBlock>
            </Border>
            <Border Grid.Column="1">
                <TextBlock FontSize="16" FontWeight="Bold">Player Name</TextBlock>
            </Border>
            <Border Grid.Column="2" >
                <TextBlock FontSize="16" FontWeight="Bold">Score</TextBlock>
            </Border>
        </Grid>

        <!--Data-->
        <Border Grid.Row="2" BorderBrush="Black" BorderThickness="1">
        <ScrollViewer Grid.Row="2">
            <StackPanel>
                <ItemsControl x:Name="leaderBoardData">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock  Grid.Column="0" Text="{Binding Rank}"/>
                                <TextBlock  Grid.Column="1" Text="{Binding PlayerName}"/>
                                <TextBlock  Grid.Column="2" Text="{Binding HighScore}"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
        </Border>
        <!--Buttons-->
        
        <Button Grid.Row="3" Margin="100,5,100,5" x:Name="backToMenuBtn" Click="backToMenuBtn_Click">Back to Menu</Button>

    </Grid>
</UserControl>
