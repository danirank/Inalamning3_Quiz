<UserControl x:Class="QuizInlamning3.View.PlayerSetup"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuizInlamning3.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <!--Rubrik-->
        <Border BorderBrush="Black" BorderThickness="2" Margin="10">
            <TextBlock Text="Choose name" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="40" />
        </Border>
        <!--Namnfält-->
        <Border Grid.Row="1" BorderBrush="Black" BorderThickness="2" Margin="10">
            <TextBox  x:Name="playerName" VerticalAlignment="Center" HorizontalAlignment="Center" Width="300" Height="50" FontSize="30" />
        </Border>
        <!--Knapp-->
        <Border  Grid.Row="2"  BorderBrush="Black" BorderThickness="2" Margin="10">
            <Button x:Name="playerNameBtn" Click="playerName_Click" Content="Start Quiz"/>
        </Border>
    </Grid>
</UserControl>
